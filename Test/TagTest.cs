using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Test
{
    
    public class TagTest
    {
        [TestMethod]
        public void ListPreference()
        {
            /*
            Bll.Tag bllTag = new Bll.Tag();
            List<Model.Tag> tagList = bllTag.GetSystemTag(Model.Enum.enSystemTagType.USER_PREFERENCE);
            Assert.AreNotEqual(tagList, null);
             */
        }

        [TestMethod]
        public void ListWidgetCategory()
        {
            /*
            Bll.Tag bllTag = new Bll.Tag();
            List<Model.Tag> tagList = bllTag.GetSystemTag(Model.Enum.enSystemTagType.WIDGET_CATEGORY);
            Assert.AreNotEqual(tagList, null);
             */
        }
    }
}
